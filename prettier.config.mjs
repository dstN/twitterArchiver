const config = {
  printWidth: 80,
  tabWidth: 2,
  singleQuote: false,
  plugins: ["prettier-plugin-tailwindcss"],
  proseWrap: "always",
  singleAttributePerLine: true,
  bracketSpacing: true,
}

export default config;
